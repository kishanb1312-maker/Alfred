# Secrets & local config
.env
config/search.yaml
config/profile.yaml

# Tailored application artifacts (generated per job)
output/

# Runtime data + personal files (resume, cover letters, stores) — never commit anything in data/
data/*
!data/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# OS
.DS_Store
